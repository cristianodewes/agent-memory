# Copy to docker/.env and adjust as needed.

# ---- Postgres (derived index; wiring lands in issue #4) ----
POSTGRES_USER=agentmemory
POSTGRES_PASSWORD=agentmemory
POSTGRES_DB=agentmemory

# ---- LLM provider (REQUIRED from issue #6 onward; unused in scaffolding) ----
# AGENT_MEMORY_LLM_PROVIDER=anthropic
# AGENT_MEMORY_LLM_API_KEY=
