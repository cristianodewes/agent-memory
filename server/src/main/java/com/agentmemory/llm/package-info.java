/**
 * The LLM provider auth boundary plus the {@code LlmProvider} / {@code Embedder}
 * interfaces. The LLM is a REQUIRED dependency (design-decision DD-005).
 * (See docs/ARCHITECTURE.md §6.)
 *
 * <p>Scaffolding package — implementation arrives with issue #6.
 */
package com.agentmemory.llm;
