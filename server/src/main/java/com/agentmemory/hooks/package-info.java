/**
 * Hook payload schemas, the sanitizer (typed privacy boundary), and the /hook
 * ingress. (See docs/ARCHITECTURE.md §6.)
 *
 * <p>Scaffolding package — implementation arrives with issues #7, #8 and #9.
 */
package com.agentmemory.hooks;
